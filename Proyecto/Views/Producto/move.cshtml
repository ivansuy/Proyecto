@using Proyecto.Models;
@{

    var lista = new List<movimiento>();

    if (ViewBag.movi != null)
    {
        lista = (List<movimiento>)ViewBag.movi;
    }

}
<form class="Producto-form" method="post" action="/Producto/Guardarmovimiento">
    <div class="form-group">

        <label for="username">Tipo Movimiento</label>

        <input type="text" class="form-control" name="Nombre" placeholder="Ingresa el tipo de movimiento" required>
    </div>
    <button type="submit" class="btn btn-primary btn-block">Enviar datos</button>
</form>

<div>

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Nombre</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in lista)
            {
                <tr>
                    <th scope="row">
                        @item.Id



                    </th>
                    <td>
                        @item.Nombre
                    </td>

                </tr>
            }



        </tbody>
    </table>
</div>
